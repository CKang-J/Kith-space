# superpowers

本目录为历史设计规格的归档容器。其下 `specs/` 中的阶段性产品 / 架构 / UI 设计文档已全部移入 `docs/archive/specs/`，**不再逐条作为当前事实**。

若需查阅某阶段"当时怎么设计"，进入 `../archive/specs/` 找对应日期命名的文档。当前架构、UI 与决策的权威来源是 `docs/index.md` 指向的核心文档（vision / decisions / architecture-proposal / ui-direction）。
