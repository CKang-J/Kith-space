node_modules/
dist/
web/dist/
.env*
!.env.example
!.env.docker.example
*.log
logs/
.DS_Store
.claude/settings.local.json

# personal / machine-local dev context — never published
CLAUDE.local.md

# defensive: never let private lab material into the public repo
reference/
recon/
_vendor/

# screenshots / browser-verification captures — local only
.shots/
artifacts/

# SQLite runtime data — workspace/registry databases are user data, never in the repo.
# (drizzle/ migrations ARE tracked — they are the schema source of truth.)
*.db
*.db-shm
*.db-wal
.kith/

.agents/skills/
